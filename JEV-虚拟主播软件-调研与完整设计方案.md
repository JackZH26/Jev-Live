**JEV Studio：开源游戏虚拟主播软件——调研与完整设计方案**

[简体中文](JEV-虚拟主播软件-调研与完整设计方案.md) · [English](docs/DESIGN.en.md)

**作者：JackZH26 · 𝕏 [@jackzhj](https://x.com/jackzhj)，欢迎关注交流。**

**需求更新：产品面向 Steam 游戏库。** 用户先从本机 Steam 库添加游戏，再选择直播游戏并通过 Steam 启动；首测 **Enter the Cube Playtest（5272970）**。普通主播不需要开发工程、游戏源码或 Unreal 编辑器。目标是扩展到任意游戏，但可直播与自动操作支持分开标注。第一阶段调整为 Steam 游戏选择、Playtest 自动游玩与 YouTube/Twitch/X 推流；角色、语音、聊天后续接入。界面首期简中、繁中、日、韩、英五语言，说明文档中英双语；原创软件已确定 MIT。下文完整产品架构包含未来阶段，实际实现和验证以[验收记录](docs/PHASE1_STATUS.md)为准。

调研日期：2026-09-20。目标首发系统：Windows。首个适配对象：截图中的 Enter the Cube / Enter the Cube Playtest。产品名 JEV Studio 为工作代号，发布前需要检查名称与商标。

**结论与推荐路线**

可以建设这套软件，但在本次查阅范围内，没有发现一个已验证同时满足“任意 Steam 游戏自主游玩、虚拟角色自然解说、五个平台双向聊天、独立直播布局、24 小时恢复、一键配置”的开源成品。最合适的路线是：以 AIRI 的角色、语音和前端组件为基础，新建直播工作台；结合 JEV 的结构化决策、针对游戏的执行适配包、OBS 的采集与合成、多平台连接器和独立的运行监督程序。

产品提供“自动玩”和“手动玩”两种正式模式。完整版本的虚拟角色、自动解说、自动语音回复、自动文字回复、聊天布局与多平台直播能力完全共用，唯一的功能差别是游戏由 AI 还是用户操作。文字回复仍以各平台已有权限为前提，这个限制对两种模式一致。游戏开发权允许未来增加可选状态接口，但不作为主播使用软件的前提；当前第一阶段优先完成 Steam 游戏选择与游玩、多平台推流；X 使用官方 Live Studio RTMPS 源，事件和可见性由 X 后台管理，详见 [X 配置](docs/X_SETUP.md)。

用户选择游戏后再选择操作模式。支持自动控制的游戏可任选两种模式；尚未适配自动控制的游戏仍可选择手动玩，使用相同的自动互动系统。游戏解说依据可获取的游戏状态或视觉观察，状态不足时只回应已确认的信息。自动控制适配的缺失不会阻止普通聊天与语音互动。

**双模式产品约定（本次需求更新）**

| 功能 | 自动玩 | 手动玩 |
|---|---|---|
| 移动、镜头、攻击、交互和游戏菜单操作 | AI 决策与执行 | 用户键鼠／手柄操作 |
| 游戏状态观察与事件理解 | 自动 | 自动 |
| 主播解说、语音回答观众 | 自动 | 自动 |
| 支持平台的文字回复 | 自动 | 自动 |
| 角色表情、口型与字幕 | 自动 | 自动 |
| 聊天布局、多平台推流与直播故障恢复 | 共用同一套功能 | 共用同一套功能 |

直播中可以切换模式，切换不会重启直播、角色或聊天连接。手动玩是完整的日常使用模式；用户可以只负责玩游戏，由虚拟主播持续自动主持直播。

三个决定架构的事实：JEV 目前不接受图像，不生成自由文本；多平台推流和多平台聊天是不同能力；Twitch 的同步直播规则限制跨平台聊天在其直播画面中的聚合展示。因此，需要独立的游戏状态层、主播语言层，以及按输出目标分别生成的画面。[JEV 官方能力说明](https://docs.typesafe.ai/concepts/system-one)、[Twitch 同播条款](https://www.twitch.tv/p/terms-of-service#11-simulcasting)

本文件中的功能架构、参数、阶段和验收指标是设计建议；GitHub 能力、许可证和平台规则是本次查询结果；没有执行真实直播、游戏控制、付费模型压测或长期稳定性测试。附件 `prototype/index.html` 是可操作的布局概念原型，不连接账号、不启动 OBS，也不代表已经完成软件。

**1．GitHub 调研与选型**

下表的日期是 GitHub API 返回的仓库 `pushed_at` 日期，仅说明仓库有推送活动，不代表正式版本发布时间、默认分支最后提交时间或稳定性。原始字段摘要保存在 `research/github-snapshot-2026-09-20.json`。项目功能以 README 与部分源文件核查为依据，未逐一安装运行。

| 项目 | 已有能力及可复用部分 | 缺口与本方案采用方式 | 许可证／最近推送 |
|---|---|---|---|
| [AIRI](https://github.com/moeru-ai/airi) | Live2D、VRM、语音管线、多模型提供商、角色状态；有 Minecraft 等游戏集成与其他游戏实验 | **主基础**。复用角色舞台、音频与角色领域组件；新增直播工作台与监督程序。其游戏实验不能等同于通用 Steam 支持 | MIT；2026-09-20 |
| [Open-LLM-VTuber](https://github.com/Open-LLM-VTuber/Open-LLM-VTuber) | Live2D、实时语音、打断、视觉感知、主动发言、本地模型选项 | 语音链路备选与对照；与 AIRI 同时常驻会增加重复模块，不作为第二套主程序 | LICENSE 正文为 MIT，Live2D 示例资产另有许可；2026-05-15 |
| [Ikaros-521/AI-Vtuber](https://github.com/Ikaros-521/AI-Vtuber) | 最接近中文“AI 直播运营”的参考，声明有多平台弹幕、TTS、虚拟形象与互动集成 | 参考平台事件处理和直播节奏；需要逐个复测连接器，不能把 README 平台名单直接当成兼容保证 | GPL-3.0；2025-07-29 |
| [Social Stream Ninja](https://github.com/steveseguin/social_stream) | 多站点消息汇集、展示与浏览器集成 | 作为可选兼容连接器；正式主路径优先官方 API。浏览器页面变动是维护成本 | GPL-3.0；2026-09-18 |
| [OBS WebSocket](https://github.com/obsproject/obs-websocket) | 远程管理场景、源、音频、推流状态等 | 必选控制接口。OBS 28 起内置，不能误当成自带五路独立画面输出 | GPL-2.0；随 OBS 兼容矩阵锁定 |
| [obs-multi-rtmp](https://github.com/sorayuki/obs-multi-rtmp) | 多目标推流 | 适合同画面分发试验；不把它视为独立聊天布局、平台建播、账号授权和聊天 API 的解决方案 | GPL-2.0；2026-08-01 |
| [Aitum Vertical](https://github.com/Aitum/obs-vertical-canvas) / [Multistream](https://github.com/Aitum/obs-aitum-multistream) | 附加画布与多输出；Vertical 源码已有多个画布对象及 WebSocket vendor 接口 | **画面输出重点基础**。验证多横屏加竖屏、独立音轨、状态监测；补齐产品所需接口，不宣称现成即可五路全自动 | GPL-2.0；分别 2026-08-24、2026-05-19 |
| [MediaMTX](https://github.com/bluenviron/mediamtx) | 流媒体接入、转发、协议转换、记录等 | 可选自托管中继；本次官方文档已有原生转发。不承担账号登录、聊天、角色或游戏决策 | MIT；2026-09-19 |
| [blivechat](https://github.com/xfgryujk/blivechat) | OBS 内嵌 B站评论、样式与礼物处理；README 有身份码流程 | B站覆盖层和接入流程参考，统一事件进入工作台自己的布局系统 | MIT；2026-08-19 |
| [TikTok-Live-Connector](https://github.com/zerodytrash/TikTok-Live-Connector) / [TikTokLive](https://github.com/isaackogan/TikTokLive) | 非官方 TikTok LIVE 事件读取 | 实验连接器。作者明确提示非生产级逆向接口；签名服务和连接稳定性另行验证，不能保证文字发送 | 当前许可证含 AGPL 与附加条款，应按锁定版本全文复核 |
| [jev-browser](https://github.com/jkudish/jev-browser) | 根据网页可访问元素，由 JEV 选择有限操作；预算、停止条件、日志 | 浏览器检查与引导的参考；不具备 Steam 原生游戏控制链路 | MIT；2026-09-19 |
| [Cradle](https://github.com/BAAI-Agents/Cradle) | 截图感知、键鼠执行、技能管理，带若干游戏案例 | 后续无游戏源码时的视觉适配参考；不是通用开箱即玩的替代品 | MIT；2024-11-07 |
| [Voyager](https://github.com/MineDojo/Voyager) | Minecraft 的技能积累与自动任务研究 | 借鉴技能包与评价方法；不作为 FPS 控制或直播底座 | MIT；2024-04-03 |

推荐采用 AIRI，是因为它同时接近虚拟角色、语音交互、Web 渲染和游戏代理这几条链路；从 Python 聊天机器人重建桌面产品，或从 OBS 插件重建角色系统，涉及的重复工作更多。这是架构判断，并非 AIRI 已具有所需的持续直播能力。

复用时锁定经过测试的提交，将 AIRI 内部包通过 `AvatarEngine`、`SpeechEngine`、`PersonaEngine` 包装。首选读取并复用 `stage-ui-live2d`、`stage-ui-three`、`pipelines-audio`、`core-character` 等相关实现；它们是项目内部组件，不能假定已有稳定公开 SDK。对上游的改动保持小范围，保留升级补丁和来源记录。

**2．JEV 的准确职责与游戏适配边界**

本机已注册的入口是 `@jkudish/jev-browser`，本地技能限定的是浏览器可访问控件操作。`E:\Jev` 初始没有游戏或软件源码。本次没有发现可供直接接入的 Enter the Cube 控制实现，也没有测试 JEV 在该游戏里的决策质量。

TypeSafe 官方文档目前提供 Choice、Score、Noul 三类输出，输入为文本、JSON 对象或文本数组；模型没有图像、音频、视频输入，也不写台词。模型概率或 confidence 不是单次判断正确的保证，特别需要用真实游戏回放校准。[官方概念](https://docs.typesafe.ai/concepts/system-one)、[模型与限制](https://docs.typesafe.ai/models)、[confidence](https://docs.typesafe.ai/confidence)

| 层次 | 职责 | 首版建议频率／方式 |
|---|---|---|
| 游戏状态采集 | 局面、玩家可感知对象、血量、弹药、事件、菜单状态 | 游戏接口事件驱动；必要状态约 10–20Hz，本地完成 |
| JEV 决策 | 从允许的动作中选择推进、撤退、补给、目标、技能或菜单动作 | 起始按 1–2Hz 或关键事件调用；实测后调整 |
| 本地动作控制 | 移动、镜头、瞄准平滑、射击时序、按键释放 | 30–60Hz，由本地控制器处理 |
| 主播导演 | 决定解说、答观众、简短反应还是安静 | 事件触发；可用 JEV 评分加确定规则 |
| 台词与语音 | 生成角色化短句，合成声音，驱动口型 | 独立 LLM 与 TTS，异步执行 |

这里的频率是实施起点，不是实测性能。供应商发布的延迟不能当成本机跨网络请求的 P95，也不足以证明 FPS 逐帧控制可行。模型无响应时必须继续运行本地安全策略，不能等网络返回再释放开火键。

**Enter the Cube 适配包**

正式入口扫描本机 Steam 安装库，用户添加并选择游戏，通过 Steam 协议启动安装版并识别真实游戏进程。首期为 Playtest 实现外部窗口观察、OCR 状态和有限键鼠动作；OCR 不等于完整视觉理解，可靠瞄准与场景导航仍需视觉能力。未来若游戏正式版本自带显式启用的 `ETC Game Bridge`，可作为可选增强接口；接口只输出角色可感知状态，不默认读取隐藏敌人位置。普通用户不需要源码或编辑器。首批在机器人局、专用测试房间或明确允许自动化的环境验证。

状态包括 `session_id`、`state_seq`、游戏时间、大厅／战斗／死亡／结算／加载阶段、生命与资源、可见目标、允许动作、最近事件。动作包括选择目标、移动到合法位置、攻击、换弹、交互、重生、下一局等有限枚举。动作实际效果需由回执和后续状态验证。

每次动作请求必须带观测版本、截止时间、动作 ID 和控制权版本 `controlEpoch`。执行前检查当前游戏实例、角色存活状态、动作前提，以及当前确为自动玩且控制权版本一致；过期结果丢弃，重复 ID 不重复执行；短期动作租约到期自动释放输入。失去游戏窗口焦点时暂停键鼠路径，不向其他应用发送输入。

游戏适配接口分为只读的观察与可写的动作两部分。两种模式都保留观察；只有自动玩启用动作决策与执行。手动玩关闭键鼠／手柄注入、游戏 Bridge 写指令及自动菜单操作。主播导演仍可使用 JEV 做话题选择、评论评分等判断，不会因游戏模式切换而停止。

自动切手动时：先撤销 AI 控制权、递增 `controlEpoch`，取消待执行与在途动作，释放由 AI 持有的输入，再显示“手动玩”。动作执行端也必须拒绝旧版本请求，防止延迟返回的结果抢回控制。只释放 AI 持有的输入，不干扰用户本来按住的键或手柄。

手动切自动时：确认游戏支持自动控制、窗口和状态可用，获取最新观测并建立新的控制权版本后再接管；检查失败则保持手动玩并提示原因。提供可自定义的“切到手动玩”快捷键。模式、直播启停、自动控制暂停和主播静音分别保存状态；切换模式保留角色记忆、回复队列、音频播放、静音设置与平台直播 ID。

以下是产品内部协议示意，不是 TypeSafe API 的原始请求格式：

```json
{
  "sessionId": "example-session",
  "controlMode": "auto",
  "controlEpoch": 7,
  "stateSeq": 381,
  "phase": "combat",
  "player": {"hp": 28, "ammo": 4},
  "facts": {"visibleThreats": 2, "coverReachable": true},
  "allowedActions": ["take_cover", "reload", "engage_visible_target"],
  "deadlineMs": 500
}
```

适配包还应包含：Steam AppID 与可执行程序识别、构建版本范围、启动方式、常见弹窗与菜单、训练关卡、卡死判断、重连和新一局流程、HUD 避让区域、测试回放集。AppID 不从截图猜测，在读取本机 Steam 安装清单后确认。

**其他游戏的支持等级（与操作模式分别标注）**

| 档位 | 来源 | 对用户的实际承诺 |
|---|---|---|
| 已验证自动控制 | 官方接口、开发者 Bridge、允许的 Mod 或充分测试的控制包 | 支持指定游戏版本；自动玩、手动玩均可选择 |
| 实验性自动控制 | 视觉模型／OCR 将截图变成状态，再交给 JEV 与本地技能执行 | 自动玩需要校准分辨率、键位和 UI；手动玩使用相同的自动互动系统 |
| 未适配自动控制 | 游戏画面可采集，观察能力按实际可用接口／视觉方案标注 | 可选择手动玩并自动互动；自动玩按钮不可用并说明原因 |

“通用”由适配包生态实现。不能把一个提示词作为所有 Steam 游戏的兼容层。游戏更新后运行回放与启动检查，失败则退回实验状态，而不是继续显示绿色认证。

**3．整体架构和技术选择**

```text
用户键鼠／手柄 ─────────────→ Steam 游戏（手动玩）
JEV 动作决策 → 控制权检查 → 游戏执行器 → Steam 游戏（自动玩）

Steam 游戏 → 只读状态／画面观察 → 游戏事件 ──┐
                                            ↓
各平台聊天 → 各自连接器 → 统一事件队列 → 主播导演 → LLM 台词
                                            │           ↓
                                      文字回复路由   流式 TTS
                                            │           ↓
                                            │    AIRI 角色／口型
                                            ↓           ↓
游戏采集 ─────────────────────────────→ OBS 多画布合成
                                             ↓
                        各平台独立输出 → 直推或可选自托管中继

桌面工作台：账号、角色、游戏、自动玩／手动玩、布局、开播控制
独立监督程序：状态机、心跳、预算、恢复、日志、停止
```

| 部分 | 建议实现 | 理由 |
|---|---|---|
| Windows 桌面工作台 | Electron + Vue + TypeScript，延续 AIRI 技术栈 | 减少跨栈重写，兼容 Web 角色舞台与布局组件 |
| 核心调度与事件 | TypeScript 服务进程，带类型的本机消息接口 | 首版统一语言，连接器可单独重启 |
| 布局编辑器 | DOM／Canvas 选择与变换；统一布局数据模型 | 预览、导出与 OBS 使用同一坐标与变换定义 |
| 持久数据 | SQLite，版本化配置与布局，受限容量的事件日志 | 免部署数据库，支持重启恢复和迁移 |
| 监督程序 | 独立常驻进程，Windows 登录会话启动 | UI 关闭后仍可运行；与 UI 一起崩溃不会同时失去监督 |
| 游戏连接 | 只读观察接口 + 带控制权检查的动作接口 | 两种模式共用观察，手动玩禁止自动执行动作 |
| OBS 控制 | WebSocket v5 + 经验证的 Aitum 适配／扩展 | 原生采集与硬件编码；避免重造整个录播器 |
| 角色渲染 | AIRI 相关组件；VRM 与 Live2D 两种适配 | 默认一个已授权角色即可使用 |
| 本地语音选项 | 独立 Python TTS 工作进程，打包依赖 | 普通用户不需要安装 Python 或修改 YAML |
| 网络中继 | 可选 MediaMTX + 独立输出工作进程 | 局部网络故障隔离；默认不要求租服务器 |

首版只承诺 Windows。公开平台／游戏／语音适配接口，为后续 macOS、Linux 做准备，但 Steam 游戏与采集兼容性要分别验证。

控制平面与媒体平面分开：聊天连接器故障不停止游戏，JEV 故障不停止声音采集，UI 关闭不停止已经开始的直播。OBS 是媒体核心，游戏和主播语音各自保持独立音轨。

**4．普通主播的配置流程**

第一次启动提供五步向导；参数自动推荐，不让用户先学习 RTMP、OBS 场景和模型部署。

1. **连接平台。** 点击 YouTube、Twitch 等图标，跳转系统浏览器授权；需要推流地址的平台才显示地址与密钥入口。每个平台清楚列出“能开播／能收聊天／能发文字／能自动恢复”。
2. **添加 Steam 游戏并选择操作模式。** 扫描本机 Steam 已安装游戏，用户先添加、再选择直播游戏，显示版本和自动控制适配状态，通过 Steam 启动。首测 Enter the Cube Playtest（5272970）。选择“自动玩／手动玩”，并说明完整版本两种模式都自动语音和文字互动。未适配自动控制时仍可选手动玩；不提供开发工程或编辑器作为普通主播入口。
3. **选择主播。** 内置已获再分发许可的默认角色，选声音、语言、性格和话多程度；可导入 VRM／Live2D 与角色包。
4. **调整画面。** 默认全屏游戏、左下角色、右侧聊天，拖动与缩放即可。可切到每个平台实际输出预览，以及横屏／竖屏模板。
5. **试播检查。** 播放试听，验证捕获的是游戏窗口，检查画面和音频、账号权限与预算；先本地预览，用户点击“开始直播”后才对外输出。

完成配置后，日常首页只保留：主播、游戏、自动玩／手动玩、目的平台、开始直播。记住用户上次选择的模式；运行中在首页和浮动控制条都能切换。高级选项收进侧栏。性能检查、Token 更新、角色启动、用户请求的游戏启动、OBS 场景创建、平台建播与推流由启动流程编排。手动玩无需等待自动控制适配检查通过。

“一键”从已经具备直播资格、账号授权有效、捕获与互动链路可用开始；选择自动玩时额外要求自动控制适配检查通过。首次直播开通、验证码、平台审核、账号重新授权不能被软件消除。YouTube 官方提示首次开启直播可能等待最多 24 小时。[YouTube 编码器开播](https://support.google.com/youtube/answer/2907883)

开播结果不必同时成功：例如显示“3 个平台已开播；TikTok 待权限；X 待建播”。首次配置可选择“有可用平台就开始”或“指定主平台必须成功”；保存该偏好，日后不反复询问。禁用的平台不阻止已授权的平台运行。

**真正简化账号配置的产品工作**

官方发行版由维护者注册和维护平台应用，在适用时完成 OAuth 审核与配额申请；桌面端使用各平台支持的公共客户端授权流程，需保密的客户端凭证放在可选授权服务中。不能把平台 client secret 放进安装包，再要求每个普通主播去开发者后台建项目。[Google 原生应用 OAuth](https://developers.google.com/identity/protocols/oauth2/native-app)、[Twitch 授权方式](https://dev.twitch.tv/docs/authentication/getting-tokens-oauth/)

完整自托管发行版允许自带应用 ID、授权服务和模型服务，但会有一次部署配置。开源与“无需维护任何服务”不是同一承诺。官方发行版的授权服务也应提供源码，并允许用户替换。

**5．平台接入矩阵**

以下描述接口可行性，不表示已确认你的账号具有相应权限。各项能力分开探测、分开标注官方／实验／不可用。

| 平台 | 推流与建播 | 聊天接收 | 文字回复 | 首版产品状态 |
|---|---|---|---|---|
| YouTube | 已开通直播的频道可使用编码器；授权 API 可编排直播资源 | 官方 Live Chat API，优先评估 `streamList`；回退轮询须遵守间隔和配额 | 官方 `liveChatMessages.insert`，需相应授权与限流 | 第一优先 |
| Twitch | 官方登录、推流凭证、直播状态；按需配置频道资料 | 官方 EventSub | 官方 Send Chat Message API | 第一优先；输出画面单独过滤聊天来源 |
| B站 | 直播间资格与推流凭证另行验证；能拿到 RTMP 不代表拥有全套建播 API | 优先官方直播开放平台，结合应用资格、主播身份码和项目授权实测 | 本次没有确认通用且适用于本产品的官方发送链路 | 首批接入收弹幕与语音回答；文字功能按权限开放 |
| TikTok | 依账号／地区与 LIVE 权限；只有获得有效外部推流入口时才能直推 | 常用开源库为非官方逆向；可选第三方服务，不能标成官方接口 | 没有核实可对所有普通账号稳定使用的官方聊天发送接口 | 条件支持、实验接入；缺权限时不承诺无人值守 |
| X | 官方 Live Studio RTMPS 源已接入并完成真实私密收流；事件在 X 后台创建 | 本次未核实适用于普通开发者的通用官方 LIVE chat 接口 | X 帖子回复不等同于直播聊天；不混用 | 优先视频输出，互动保留能力插槽与未验证状态 |
| 其他 RTMP 平台 | 用户提供官方有效入口即可评估视频推送 | 需单独连接器 | 需单独连接器 | 可扩展；不能写成“自定义 RTMP = 全功能平台” |

来源：[YouTube LiveChatMessages](https://developers.google.com/youtube/v3/live/docs/liveChatMessages)、[Twitch 收发聊天](https://dev.twitch.tv/docs/chat/send-receive-messages/)、[B站直播开放平台](https://open-live.bilibili.com/)、[TikTok LIVE Studio 权限说明](https://www.tiktok.com/live/studio/help/article/Before-you-go-LIVE/Apply-for-LIVE-access?lang=en)、[TikTok 非官方连接器声明](https://github.com/zerodytrash/TikTok-Live-Connector)、[X Live Studio](https://help.x.com/en/using-x/live-studio)

TikTok 部分帮助页受抓取限制，已检索到官方权限说明，但未进入用户账号实测。B站文档站部分页面依赖登录或前端渲染，不能据此推断所有接口开放。自动化权限与服务政策需要在实际发行和每个平台上线前再次核对，不承诺所有平台都允许无人值守的相同运营方式。

读到评论却无法发送文字时，可以通过直播声音回答，并在自有字幕层展示回答；界面明确写“语音回复”，不能冒充“文字已发送”。收不到某平台评论时，该平台仍可展示视频，但不生成假聊天和假互动。

**6．OBS、多平台独立画面与横竖屏**

截图二的核心视觉是游戏主体、左下半身虚拟主播和右侧竖排聊天。默认模板复用这种信息结构；不复制截图里角色的美术资产。

需要区分三个概念：后台管理的聚合聊天、直播画面中的聊天组件、观众在平台网页／App 上的原生聊天界面。软件能调整第二种，不能改变观众 App 自带聊天区的位置和大小。

每个平台聊天作为独立图层，可单独选择、拖动、缩放、锁定、隐藏；每个图层设置字体、透明度、消息条数、停留时长、礼物展示、过滤规则和输出目的地。显示“平台全部消息”与“主播当前回答的问题”可使用两个不同组件。

| 输出视图 | 默认画面 | 聊天规则 |
|---|---|---|
| YouTube | 16:9，游戏完整画面、角色、字幕 | 默认 YouTube 聊天；其他平台可作为独立图层配置，须核对其规则 |
| Twitch | 16:9，与主平台相当的画质和互动待遇 | 仅 Twitch 聊天和相关提醒；不烧录其他平台聊天／关注礼物活动 |
| B站 | 16:9，中文字幕与角色 | 默认 B站弹幕列表，可选适合画面的滚动方式 |
| TikTok | 9:16，游戏上方等比显示或按游戏设置裁切，角色与聊天在下方 | 默认 TikTok；布局预留平台按钮和字幕遮挡区域 |
| X | 经验证的横屏编码预设 | 互动连接未可用时隐藏聊天组件 |
| 管理员预览 | 本地汇总各平台消息 | 可聚合，但不是默认公开输出 |

Twitch 官方 FAQ 明确区分了私人使用聚合工具与把跨平台活动合并进直播画面。分几个聊天框显示，也不能自动解决这个限制。规则应在输出配置校验中生效，而非依赖用户记住。[Twitch 同播 FAQ](https://help.twitch.tv/s/article/simulcasting-guidelines?language=en_US)

**建议输出实现**

游戏源和角色源尽量只采集／渲染一次，作为共享源放进多个场景。每个目标场景组合自己的聊天和字幕图层。通过 Aitum 多画布与输出的适配层，建立独立渲染与编码。源码已核查到 Vertical 的 `aitum-vertical-canvas` vendor，以及场景切换、状态、启停推流、更新地址和密钥等接口；尚未验证这些接口能覆盖五个目的平台的全部产品需求。[源码入口](https://github.com/Aitum/obs-vertical-canvas/blob/main/vertical-canvas.cpp)

因此前置验证必须检查：能否按唯一 ID 管理多个横屏与竖屏画布；是否能创建、更新、删除画布和图层；各自编码、音轨、错误与重连状态是否可控。缺失部分作为 GPL 插件扩展开发，不能假定普通 OBS `StartStream` 调用会启动所有输出。

如果插件路径不能满足稳定性，可暂用受管理的多个 OBS 实例作为验证或降级方案。它需要独立配置、端口与明确的共享采集方案，而且增加资源成本；正式默认架构优先收敛到经过测试的单 OBS 多画布路径。

**中继能解决什么**

同一张已经合成的视频可以编码一次再分发。若五个平台有不同聊天画面或编码参数，就需要多个独立视频变体；RTMP 转发不能在压缩后的视频上免费替换聊天框。MediaMTX 提供转发，重编码／滤镜仍要相应媒体处理程序。[MediaMTX 转发说明](https://mediamtx.org/docs/features/forward)

本地直推作为默认，减少服务器配置。可选中继提供更稳定的出口或故障提示画面，但五个本地变体仍要上传五路。真正“一路上云、五路不同画面”需要把聊天叠加、角色同步、合成和编码移到云端，增加云 GPU、浏览器渲染、字幕同步与运维成本，安排在后续版本。

**7．角色、声音与像主播一样说话**

角色包由模型、表情映射、动作、声音、人格、可讲的话题、游戏知识与素材许可组成。默认提供原创或明确可再分发的角色，用户能直接选用；导入外部角色时识别模型版本、表情与口型参数。

本节全部自动解说与互动能力适用于自动玩和手动玩，使用同一套人格、声音、节奏、审核、记忆和回复策略。手动玩时虚拟主播仍观察真实游戏并主动解说，用户无需自己说话或打字回复；角色只描述已观察到的操作，不编造玩家意图。真人麦克风若启用，作为独立音源配置，与操作模式无关。

渲染支持两条路径：内置 AIRI 的 Live2D／VRM 舞台；可选外接 VTube Studio API。后者适合已有 Live2D 工作流的主播，但 VTube Studio API 文档公开不等于应用本体可作为开源代码复用。[VTube Studio API](https://github.com/DenchiSoft/VTubeStudio)、[three-vrm](https://github.com/pixiv/three-vrm)

口型跟随真正播放的音频时钟，表情跟随结构化情绪标签。TTS 取消时同时清空尚未播出的口型和字幕。待机眨眼、呼吸、注视与轻动作由本地动画管理；多路输出共享角色动作状态，只对尺寸和位置作变换。

声音默认云服务，降低单机游戏与本地模型抢显存的风险；提供本地模式供后续选择。中文／中英混合可评估 CosyVoice；轻量语音可评估 Kokoro。代码、权重和具体声音素材的许可分别记录，不能把代码许可证替代声音授权。[CosyVoice](https://github.com/QwenAudio/CosyVoice)、[Kokoro 模型卡](https://huggingface.co/hexgrad/Kokoro-82M)

主播不是“每条弹幕调用一次 LLM”。导演根据局面决定节奏：

| 时机 | 主播行为 | 控制方法 |
|---|---|---|
| 激烈战斗 | 短反应、少解释，允许专注沉默 | 只挑关键事件；限制句长；普通问题入队 |
| 等待／加载 | 回复问题、介绍下一局计划 | 消化有效队列，过期问题丢弃 |
| 胜负结算 | 简短复盘、承认失误、提出下局目标 | 引用真实事件，不编造操作或战绩 |
| 有效提问 | 答复该观众，必要时关联游戏 | 结合游戏知识包与近期对话 |
| 大量重复评论 | 总结共同问题 | 相似内容合并；每个平台留出服务机会 |
| 冷场 | 分享下一步思路，或保留游戏声音 | 冷却时间与主题轮换，避免固定间隔机械自言自语 |

默认话量可从约 15%–30% 的发声占比开始试验，让真人评审选择舒服的节奏；这是产品参数，不是直播效果保证。提供安静、均衡、活跃三档。普通句子尽量短，关键动作中避免长篇问答。

回复链路：消息去重与过滤 → 相关度／可答性评分 → 导演选择 → 生成短台词 → 内容检查 → TTS → 音频播放与字幕 → 按权限发回原平台。后台保存实际播放与实际发送状态，区分“生成了”与“观众听到了”。同一条消息不会因为重连重复回复。

初版每次只播放一个角色声音。语言按主播主语言进行语音回答，文字可以跟随观众语言；不同时在一条音轨说五种语言。后续若按平台独立配音，需要额外语音、口型时钟和编码资源，单独排期。

记忆分为角色设定、当前游戏局面、短期话题摘要、可选择启用的观众记忆。观众 ID 必须带平台命名空间，不能把同名用户当成同一个人。默认不推断身份和敏感属性；支持保留期限和删除。文本不直接交给系统执行，观众无法通过评论改变推流密钥、安装插件或调用桌面命令。

**8．iOS 27 风格与布局编辑细节**

参考 Apple 当前的 Liquid Glass 材质与 iOS 27 对清晰度、对比度及透明度调节的处理，做适合桌面操作的原创界面。不是机械地把桌面软件画成手机，也不把玻璃效果盖在所有文字上。[Apple iOS 27 更新说明](https://support.apple.com/en-gb/149076)、[Liquid Glass 设计介绍](https://www.apple.com/newsroom/2025/06/apple-introduces-a-delightful-and-elegant-new-software-design/)

界面分为直播间、主播、游戏、平台、运行记录五个主要入口。首页和画面编辑合并，减少从配置页跳到预览页的次数。中部大画面，顶部平台输出切换，左边图层，右边所选对象属性；游戏设置显眼位置提供“自动玩／手动玩”分段选择，并固定标注“语音、文字互动始终自动”。底部浮动控制条放模式切换、暂停自动操作、静音和结束。手动玩时隐藏暂停自动操作按钮，显示“你操作游戏，AI 自动解说与回复”；自动玩时显示“AI 操作游戏，AI 自动解说与回复”。

视觉规范建议：深石墨与浅雾白主题，低饱和蓝紫色环境光；玻璃只用于导航、浮动控制条和短卡片；长文字、错误和日志使用稳定底色。卡片圆角约 20–28px，主操作胶囊按钮，交互动画约 160–240ms。支持减少动态效果和降低透明度，平台状态除颜色还使用文字。

直播布局保存的是归一化坐标与锚点，便于从 1920×1080 迁移到其他尺寸。示例模板而非强制固定值：

| 元素 | 1920×1080 默认建议 | 可调整能力 |
|---|---|---|
| 游戏 | 全画布等比显示 | 裁切、适应、填充、HUD 安全区 |
| 角色 | 左下，约画布宽 18%、高 42% | 拖动、等比缩放、裁切、镜像、锚点、图层顺序 |
| 聊天 | 右侧，约宽 22%、高 52% | 每个平台独立大小／位置／字体／透明度／可见输出 |
| 主播字幕 | 底部中间，避开游戏 HUD | 两行上限、样式、字幕语言和停留时间 |
| 直播提示 | 顶部安全区域 | AI 主播标识、当前阶段、恢复提示 |

五个平台的聊天框可在允许的输出视图中同时配置，但默认只启用当前平台的框，避免遮挡游戏；Twitch 输出不允许外平台框。拖动提供吸附线、边距提示、锁定、撤销／重做、键盘微调、数值输入和复制布局。对每个输出单独预览“观众实际看到什么”，避免把管理员的聚合面板误当作直播画面。

附带原型演示自动玩／手动玩选择与直播中切换、共用自动互动状态、平台切换、图层选择与移动、尺寸与透明度、横竖屏、布局导出和模拟开播状态；所有聊天、状态、游戏图形都是演示数据，模式切换不实际向游戏发送指令。

**9．24 小时运行与恢复设计**

24 小时是服务连续运行目标，不能假定每个平台的单次直播会话无限长。全局节目会话与各平台直播 ID 分开管理，某个平台续播或重新建播时，游戏与其他平台继续运行。

两种模式都支持持续直播与自动互动；手动玩期间的游戏进度由用户推动。用户离开时系统保持当前模式，除非用户主动选择自动玩，否则不自行接管游戏。直播、聊天与媒体恢复规则共用；涉及游戏动作、重生、菜单或自动重启游戏的恢复操作遵守当前游戏控制权。

启动状态机：空闲 → 检查条件 → 准备游戏／角色／媒体 → 验证预览 → 启动各平台 → 运行；发生故障后进入局部降级或恢复，结束时先停止对外输出，再保存状态并释放输入。恢复要区分用户主动结束、平台结束和异常掉线，不能把用户点“结束直播”后自动重新开播。

| 故障 | 自动处理 | 达到重试上限后 |
|---|---|---|
| JEV 超时／限流 | 自动玩丢弃过期动作，使用本地策略或暂停；两种模式的互动评分按共用规则降级 | 保留直播和可用互动；手动玩不受动作决策失败影响 |
| 游戏卡死／崩溃 | 共用恢复画面和自动说明；自动玩可重启指定游戏并恢复菜单，手动玩等待用户恢复或明确请求重启 | 停止循环重启，提示用户；不把桌面弹窗播出去，不自动改变模式 |
| LLM 故障 | 简短的已审核恢复说明，清空过期回应 | 游戏继续；避免重复播同一句 |
| TTS 故障 | 保留文字字幕、口型回到静止；必要时切备用服务 | 进入字幕模式，显示声音不可用 |
| 单平台掉线 | 独立重连、更新凭证或重新建播，保留聊天去重游标 | 仅该平台停用，其他继续 |
| OBS 退出／无画面 | 监督程序重启专用配置并核对所有源；检测黑帧／冻结帧与音频 | 明确报错；只有预先部署的云中继可在主机失效期间独立显示恢复画面 |
| 磁盘不足 | 停止非必要记录；按既定期限清理本软件缓存 | 不能静默删除其他录屏和用户文件 |
| 网络断开 | 停止积压语音；保留局面；恢复后重新校验平台会话 | 依策略暂停自动操作和输出；手动玩不注入游戏暂停或其他指令 |
| Windows 重启／断电 | 已配置的启动机制在可用登录会话中恢复 | 单机不能保证断电和登录不可用时仍直播；高可用需第二节点 |

Windows 原生游戏与捕获需要交互式用户会话，不能只部署一个 Session 0 服务就承诺重启后完全恢复。首次安装应解释运行条件，检测睡眠、锁屏、显卡驱动、音频设备切换等情形；不偷偷修改 Windows 自动登录和密码安全配置。

YouTube 超过 12 小时的直播可能无法完整归档；可提供“持续一个直播间，保留本地分段录制”与“约 8–10 小时分场以利归档”两种策略。分场意味着链接、聊天 ID 和观众连续性变化，不包装成无缝直播。[YouTube 归档规则](https://support.google.com/youtube/answer/6247592)

主机监督默认每 2–5 秒采集各模块心跳，精确周期由资源预算决定。平台“已开播”需要平台状态或实际播放证据，不能只看 OBS 正在输出字节。指标包括观众侧可播放、视频冻结、音频静默、游戏进度、响应延迟、队列长度、内存和显存趋势、成本。

录制使用当前 OBS 支持并经验证的抗中断格式，按约 30–60 分钟切片，保存后可导出 MP4；不将单个脆弱长文件作为 24 小时唯一备份。维护更新只在受控窗口执行，保留上一版本与配置迁移回滚。

**10．权限、密钥与平台内容约束**

密钥保存在 Windows Credential Manager／DPAPI 等系统密钥存储，配置文件仅引用凭证 ID；导出角色和布局不带账号、Cookie 或 Token。OBS WebSocket 仅在受控本机网络可达，并启用认证。模型请求拿到的是必要局面与待答内容，不包含推流密钥。

连接器和游戏包具有明确能力声明；安装外部包显示来源与权限。聊天内容按不可信输入处理，去除危险 HTML，禁止执行脚本和任意 URL；模型选择出的游戏动作仍需白名单与状态验证。文字发送限制频道、长度、频率和对象，重启后不批量重放积压消息。

产品默认可见地说明这是 AI 虚拟主播，并提供平台要求的内容标识入口。YouTube 对逼真的合成或重大改变内容有披露要求，但不能笼统说所有卡通头像都必须套用同一项规则。[YouTube 披露说明](https://support.google.com/youtube/answer/14328491)

“像正常主播一样说话”通过节奏、情绪、真实事件回应实现，不以冒充某个真人为前提。默认声音采用授权音色；角色、声音和背景音乐的授权跟随素材包。自动化游玩只在游戏明确允许的场景开启；商业游戏有反作弊时不尝试绕过。平台如果不允许当前账号或内容类型无人值守运行，就将其设为辅助操作模式。

**11．性能、网络与持续成本**

本次只读硬件查询显示：Core Ultra 7 265K、RTX 5070 Ti（约 16GB 显存）、约 127GiB 可见内存（通常对应标称 128GB）。这是一台可用的首测机器，但尚未验证 Enter the Cube 加五路合成编码的负载；内存充裕不能抵消 GPU 显存和编码瓶颈。

第一轮采用云端 LLM／TTS，游戏与 OBS 使用 GPU；先验证两路，再逐步增加到五路。游戏限帧，为合成预留 GPU 时间；监测显存、渲染延迟和编码丢帧，不预先保证某张显卡能在最高游戏画质下满载运行所有配置。若本地 TTS 会造成游戏抖动，移到第二台机器或恢复云端模式。

编码器配置按平台单独选择 H.264／AAC 等已验证组合，分辨率、帧率、码率、关键帧间隔不一刀切。本版本 X 预设为 1080p30、AAC 128 kbps、三秒关键帧，按实测账号面板配置；新版 Live Studio 帮助也推荐 1080p60，不再沿用旧 Producer 限制。具体按账号面板及最新文档核验。[X 编码规范](https://help.x.com/en/using-x/live-studio)

带宽算例：每路视频 6Mbps，五路为 30Mbps；加音频与协议开销后建议至少约 40–50Mbps 的稳定可用上行，并对目标地区实测。这里是规划算例，不是已测网络能力。6Mbps 每天约 64.8GB，每月 30 天约 1.94TB；五路中继的月出口约 9.72TB，尚未计音频与开销。租中继时带宽流量可能比软件费用更显著。

JEV 官方当日模型价格为每百万输入 token 0.042 美元、输出免费；限流为动态参数，不应固化成产品永久保证。下表假设每次总计费输入 1,000 token、全天恒定调用、不计额外分类请求与重试。[官方模型价格](https://docs.typesafe.ai/models)

| 调用频率 | 日调用量 | 日输入量 | 仅 JEV 日成本 |
|---|---:|---:|---:|
| 1 次／秒 | 86,400 | 86.4M token | 约 $3.63 |
| 2 次／秒 | 172,800 | 172.8M token | 约 $7.26 |
| 5 次／秒 | 432,000 | 432M token | 约 $18.14 |

采用事件触发、局面压缩、局面不变时复用结果、加载期间停止决策，可降低调用量；价格计算使用供应商返回的实际 usage，不把概率分支数当成免费无限计算。锁定模型版本，升级时重跑游戏评估。

总成本公式：JEV + 台词模型输入／输出 + TTS 时长或字符量 + 可选视觉模型 + 中继流量／计算 + 存储 + 电费。发声占比 20% 即约 288 分钟合成声音／天，应按所选声音服务计费；不能只用 JEV 单价宣传整个系统的运行费用。

上表的持续动作决策成本适用于自动玩。手动玩停止游戏动作决策调用，仍保留局面观察、主播导演、台词、语音和文字回复的用量。两种模式使用相同的互动质量设置，模式切换仅改变操作链路对应的资源消耗。

软件内展示每小时与每日预算、各模块用量、预计剩余时长。到预算阈值先减少闲聊与高成本视觉调用，再采用用户预设降级策略；无法保证自动游玩时明确暂停，避免虚报功能仍正常。

**12．开源发布和许可证组织**

新增桌面工作台与独立调度代码建议 MIT；复用 AIRI 代码保留其版权声明。OBS 与基于 Aitum 的修改按对应 GPL 版本提供源码、构建方式和 notices，单独清晰标识。GPL-2.0 与 GPL-3.0 组件不随意直接合并链接；具体发行方式按锁定版本及依赖复核。拆分进程不会自动免除全部许可义务。

Social Stream Ninja 和 AI-Vtuber 作为外部兼容方案时保留其许可，不把复制进主程序的 GPL 代码改标 MIT。TikTok 连接器当前包含附加许可条款，不按旧印象标成 MIT。Live2D SDK、示例模型和角色素材另外记录；默认角色优先采用再分发条件清晰的原创资产。[Open-LLM-VTuber LICENSE](https://github.com/Open-LLM-VTuber/Open-LLM-VTuber/blob/main/LICENSE)、[Live2D SDK 发行许可](https://www.live2d.com/en/sdk/license/)、[TikTok 连接器 LICENSE](https://github.com/zerodytrash/TikTok-Live-Connector/blob/master/LICENSE)

开放的是软件与适配协议，不意味着 JEV 云服务、TTS 服务、直播平台或第三方模型权重都开源。接口保留替换提供商的能力；基础规则策略可作为离线降级，但不宣称与 JEV 完全等价。

建议仓库按桌面应用、监督程序、角色／音频组件、游戏包、平台连接器、OBS 扩展、素材许可和测试回放分区。公开安装包、可复现构建步骤、接口版本、兼容性矩阵、更新日志和迁移工具。稳定频道锁定依赖；测试频道提前验证 OBS 和平台变化。

**13．实施阶段与交付内容**

以下是工程规模估计，假设 2–3 名具备桌面、媒体与游戏经验的开发者，另有兼职设计／测试，可修改 ETC 游戏源码；不含平台审核等待、美术制作和账号权限获批时间。先完成阶段 0 后重新估算。

| 阶段 | 预计工作周期 | 可交付成果 | 进入下一阶段的条件 |
|---|---|---|---|
| 0：打通风险最高的链路 | 1–2 周 | 手动玩加自动互动闭环；ETC 状态／动作 PoC；控制权切换；JEV 回放评估；AIRI 声音与口型；OBS 多画布样例 | 自动玩能走完整对局；手动玩无 AI 输入；切换不中断互动；输出隔离且资源可接受 |
| 1：可用的桌面 Alpha | 3–4 周 | 五步向导、双模式选择与实时切换、默认角色、ETC 游戏包、布局编辑、YouTube／Twitch 正式聊天路径、B站可行接入 | 两种模式及切换场景的 2 小时至 8 小时运行；共用自动互动、故障恢复与停止行为正确 |
| 2：全天运行 Beta | 3–4 周 | 监督程序、凭证更新、预算、平台续播、本地录像、三平台不同输出、TikTok／X 条件接入 | 24 小时与 72 小时测试通过，能力状态真实 |
| 3：可复用开源版本 | 2–4 周 | 第二款游戏包、开发者 SDK、打包更新、许可证清单、安装文档、可选中继 | 陌生用户可完成配置；适配新游戏不改核心程序 |

整体约 9–14 周是此假设下的规划区间。一个开发者应延长周期并缩小首发平台／游戏数量。全平台均有完整互动不应作为固定日期承诺，因为部分平台没有已确认的通用官方接口。

首个公开版本的可验证承诺：Enter the Cube 指定版本可选自动玩或手动玩，并能在直播中切换；两种模式都自动解说、自动语音回答、自动回复已支持平台的文字消息；一个可更换虚拟主播；支持列表中的平台一键直播；角色与各平台聊天布局独立编辑；运行失败可恢复；配置和游戏包可以复用。

**14．验收标准与前置验证问题**

| 维度 | 建议验收方法 |
|---|---|
| 简单配置 | 找未参与开发的用户，使用已具备权限的账号，从安装到本地预览争取 10 分钟内完成；外部审核等待单列 |
| 自动游玩 | 固定版本、关卡／种子和任务，持续多局无人工输入；记录成功率、卡死率、恢复率，与基础 bot 对照，不能只展示精选片段 |
| 手动游玩 | 用户键鼠／手柄正常操作；AI 游戏输入与写指令为零；相同的解说、语音和文字回复链路自动运行 |
| 模式切换 | 开播前与直播中双向切换；AI 持有输入及时释放；延迟返回的旧动作不能执行；接管检查失败保持手动；直播 ID、聊天连接、记忆、在播音频与静音状态保持 |
| 自动互动一致性 | 两种模式分别注入同样的游戏事件和观众问题，验证使用同一回复策略与权限、口型字幕正常、去重有效；切换不重复发送或清空待答问题 |
| JEV 质量 | 对实际观测状态回放评估；按动作类别测成功率、延迟和不确定性；测试断网、过期状态和限流 |
| 主播感 | 真人盲评自然度、相关性、重复率与打断；不以“台词很多”替代自然交互 |
| 回复延迟 | 初始目标从消息进入本地队列到声音开始播放 P95 小于 5 秒，平台视频分发延迟另算；游戏即时反应用本地短动作处理 |
| 平台隔离 | 在不同平台输入唯一标记，逐个检查实际输出画面与文字目标，尤其验证 Twitch 没有外平台聊天图层 |
| 画面与声音 | 每种输出检查黑边、HUD 遮挡、口型、字幕、回声、双重音频与静音；验证竖屏真实尺寸 |
| 长时间运行 | 2h → 8h → 24h → 72h，记录游戏有效运行时间、实际可播放时间和恢复画面时间，三者分别统计 |
| 故障恢复 | 主动结束游戏／连接器／OBS，注入断网、Token 失效和磁盘不足；可恢复项目目标 120 秒内恢复，账号重授权单列 |
| 安全与停止 | 评论里的越权指令不能变成系统操作；一键停止立即停发新音频、停外发、释放游戏输入；人工结束不能自动重启 |
| 复用性 | 通过新游戏包接入第二款游戏，角色与平台配置直接复用，证明不是 ETC 专用脚本包装 |

阶段 0 仍需实际验证：ETC 的 Bridge 接口与可操控范围；Aitum 对多画布及每目标编码控制的覆盖；本机五路不同输出的性能；你的各平台账号权限；B站、TikTok、X 文字发送链路；模型／素材的具体发行许可。以上不影响现在确定产品架构，但会决定首发能力矩阵。

**按更新后的优先级：先完成 Steam 库添加／选择、Playtest 自动游玩与不中断直播的模式切换，以及 YouTube／Twitch 与 X RTMPS 输出；随后接入角色和两种模式共用的自动语音／文字互动，形成两小时闭环，再扩展 B站、24 小时恢复、权限已确认的 TikTok、X 互动和第二款游戏。每一步都留下可重复的测试与观众侧证据。**
