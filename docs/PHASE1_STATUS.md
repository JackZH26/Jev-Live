# 第一阶段验收记录

[简体中文](PHASE1_STATUS.md) · [English](PHASE1_STATUS.en.md)

2026-09-20 · 0.1.0 开发预览。

> 本文保留第一阶段的历史测试记录，其中的测试数、X 帧率、旧自动玩和“虚拟人后续开发”等状态已被后续实现更新，不代表当前完整功能。最新差距见 [整体进展](REMAINING_WORK.md)，主持与新版自动玩分别见 [主持验收](HOSTING.md)、[ETC 自动玩](ETC_AUTOPLAY.md)。历史平台收流证据仍只证明当时记录的版本和范围。

当前用户流程为 **Steam 库添加／选择 → Steam 安装版游戏 → 选择 YouTube / Twitch / X 输出**。首测 Enter the Cube Playtest，AppID 5272970，已安装 candidate 构建 25364079；不需要开发工程或编辑器。

| 检查 | 结果 |
| --- | --- |
| TypeScript / Vue | `npm run check` 通过 |
| 生产构建 | `npm run build` 通过 |
| 核心及安全测试 | 39 项通过；OAuth、续期、手动接管、三路失败恢复、X 源加密／地址校验／旧配置迁移、空平台选择、YouTube 开通限制、历史密钥扫描、Steam 清单和五语言资源 |
| 平台选择 | YouTube 或 Twitch 单路无需另一平台账号；自动游玩时可勾选／取消，全部取消后禁止开播。隔离 Electron 测试验证真实 IPC、旧设置不会覆盖新选择、开播期间锁定及新增输出要求重新应用采集；五语言界面与选择持久化通过 |
| YouTube 开通限制提示 | 隔离 Electron 注入官方错误码，验证五语言提示与 Studio 入口，拒绝后两路均未启动且保留平台选择；真实频道后台仍显示首次直播开通倒计时，软件无法绕过 |
| YouTube 单路本机预演 | 打包程序加载已绑定账号，Steam Playtest 实际画面采集，30 秒内完成 13 次 JEV 有效决策；OBS 游戏音频峰值约 0.562，结束后回到手动。Twitch OBS 未启动；此项没有向 YouTube 推流 |
| Electron 界面 | 真实进程启动，工作台与设置可用，renderer 无 Node，无页面错误 |
| 三套隔离 OBS | 本机 OBS 32.2.2 三路 WebSocket 和独立编码通过；测试端口与生产端口分离 |
| Steam 库与选择 | 本机检测到 4 个可添加的已安装游戏；Playtest 从 Steam 正式启动，识别 Shipping 游戏进程；不读取账号所有者字段 |
| Steam 实机基础策略 | 一分钟测试从 BOT MATCH 进入对局，执行端收到 35 次有限操作，手动接管通过；策略被机器人淘汰，未证明竞技表现 |
| Steam 实机 JEV | 使用本机已有的加密凭证完成真实 SDK 请求，返回大厅、进入 BOT MATCH 和局内动作链路通过；另行检查实际游戏画面与输入暂停。不是开发版验证的替代说法 |
| 输入暂停与真实画面 | 最新 25 秒 JEV 测试确认 17 次执行；最小化游戏后输入释放且动作计数停止增长，恢复后手动接管通过；画面检查排除了单色测试图层 |
| 五语言与作者入口 | 真实 Electron 验证五语界面／错误、持久化、控制状态不变、1080px 设置页及 X 链接 |
| Windows 打包 | 生成独立运行目录，实际 exe 内通过界面添加／选择 Playtest 并连接本机观察程序；renderer 无 Node |
| 本地三路 RTMP | 同时发送到三个 FFmpeg 接收器：H.264 High 1920×1080，YouTube / Twitch 60fps，X 30fps，均带 AAC；OBS 三路丢帧为 0，分别约 8.8 / 9.2 / 9.6 MB。不同颜色证明画布独立；X 实测关键帧间隔三秒。此项不代替真实平台收流 |
| X 配置界面与 IPC | 隔离 Electron 验证真实 IPC：单路 X 无 OAuth 账号可准备，源加密、快照不含密钥／地址、输入提交清空、无效域名拒绝、发送期间禁止移除源；无真实平台请求 |
| X 实际私密收流 | 官方 Live Studio Private 事件收到 Steam Playtest 大厅；平台播放器解码 1280×720 版本且时间持续前进，源端 1080p30，OBS 统计零丢帧。确认画面后主动结束 X 事件并停止编码器、清除临时密钥；保留原有平台勾选，没有公开发帖。局内游玩及人工听音验收未包含在本次短测 |
| Google / Twitch 实际授权 | Google Desktop OAuth 与 Twitch Public Device Code OAuth 均已完成真实授权；Twitch 令牌及推流密钥读取权限验证通过。打包程序重启后两张账号卡片均加载成功，YouTube 私密设置、原有输出勾选及 Steam 游戏选择保留；账号绑定没有启动直播 |
| YouTube / Twitch 实际接收 | 未完成。YouTube 测试频道处于首次直播开通的约 24 小时等待；未将本地准备或 OAuth 成功当作平台收流验收 |
| 自动画面理解 | OCR 仅提供文字与菜单坐标，尚无完整敌人定位、可靠瞄准、避障和多局成功率基准；自动支持明确标为实验 |
| 24 小时稳定性 | 未执行 |
| 虚拟人、语音、聊天 | 后续阶段，界面标注未启用 |

详细截图和运行 JSON 在被忽略的 `test-results/`，不上传运行账号和会话。公开文档只保留不含凭证的汇总结论。

此前开发工程桥接的编译和 90 秒运行属于探索性历史测试，不能证明 Steam 版本的兼容性；现行验收已切换到真实 Steam 安装版。`integrations/etc` 仅保留可选开发者参考。
