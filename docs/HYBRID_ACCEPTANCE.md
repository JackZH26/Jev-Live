# 共用执行器实机验收

[English](HYBRID_ACCEPTANCE.en.md) · [架构](HYBRID_AUTOPLAY.md)

## Steam 更新已验收 · 2026-09-21

`20260921-05` 已发布为 Steam BuildID **25425783**，default 与 candidate 一致。真实 Steam 升级、137 文件校验、大厅、登录、BOT 基础操作和 M 地图均通过。详见[发布验收](STEAM_PLAYTEST_20260921.md)。下文保留此前本地候选测试历史，不代表连续两次第一已达成。

## 知识驱动守备与首场冠军 · 2026-09-21

该策略首批 6 场按顺序为第 14、18、1、14、19、6 名，全部为正式结算并确认输入释放；最终连胜 0，最长 1。后续知识版本 `etc-20260921-03` 加入短距离补给的倒计时预算，以及守备条件成立后立即取消旧过门动作的修正。177 项测试与类型检查通过；这一后续策略需单独计算实机成绩，不能继承前版胜场。

知识版本 `etc-20260921-02` 在现有 28 类房间规则上新增守备姿态：仅对已确认的安全地形房、已有主武器和足够弹药的角色启用。云端暂不选择无目的过门，本地在可见敌人、近期掉血或区域预警出现时恢复战斗与转移。未知房间和活动机关房不会获得静止安全的假设。

本地候选 `20260921-04` 的 `hybrid-smoke-1789927318106` 获得首个正式第一名：完整流程 638.9 秒，2 击杀、7 发耗弹、100 生命结算，266 次 Jev 请求 / 254 次接纳；输入释放在第三次读回确认。角色在 025 蚀光拱廊取得霰弹枪并守备，房间变为危险后撤离，随后更换冲锋枪、转移并赢得官方结算。真实 M 地图快照覆盖 P1–P5，知识与地图来源仍受玩家可见信息限制。

这不是连续两次第一：随后一场 `1789927957342` 为第 14，完整记录保留，连胜归零。没有调整 19 个 Regular 敌人的数量、难度、伤害或结算。175 项 TypeScript 测试与类型检查通过；候选原生包和画质配置保持不变。原始证据与脱敏摘录保存在本机被 Git 忽略的 `test-results`，不提交账号、密钥、桥接凭据或完整运行记录。

前一策略批次的第三场 `1789926726317` 为第 6、1 击杀、12 发耗弹、75 次请求 / 68 次接纳，输入释放通过。该场取得主武器后继续频繁换房，最终在碰碰车房死亡，是增加房间守备判断的依据；不能仅凭死亡地点断言伤害来源。

## 玩法知识接入 · 2026-09-21

已从当前 UE 规则、房间设计/实施文档和原生实现整理 001–028 房间知识；原 Godot 总策划案路径未找到，不以旧稿覆盖现行实现。知识注入、真实 M 地图查看和倒计时路线已进入本地候选。详见[玩法知识](ROOM_KNOWLEDGE.md)。

候选 `20260921-03` 连续 5 场均获得正式结算并成功释放输入；153 次云端请求都携带与当前房间标题类型一致的规则，错配为 0。实际地图截图覆盖开局、受阻重查与刷新前检查。

| 记录后缀 | 官方名次 | 发射数 | Jev 请求 / 接纳 |
|---|---:|---:|---:|
| 1789924815053 | 15 | 0 | 38 / 37 |
| 1789924896498 | 19 | 0 | 8 / 7 |
| 1789924928021 | 8 | 0 | 44 / 41 |
| 1789925060403 | 17 | 3 | 32 / 28 |
| 1789925119463 | 13 | 0 | 31 / 30 |

这些结果证明上下文和控制链路运行，不能证明战斗水平合格。记录暴露出频繁换房、弱装备、远距离逃跑时不反击，以及 011/027 坠落。后续候选 `20260921-04` 保留出口真实高度、腾空阶段延后目标切换，区分备用撤离目标与当前动作，优先补给并响应可见攻击；170 项 TypeScript 测试和类型检查通过，Shipping 编译、Cook、Stage 与 137 文件校验通过。开局前台激活增加有界重试，辅助脚本散列也进入连胜版本身份。该候选正在进行正式对局验收，尚未证明连续两次第一。

私密控制台已复验 HTTPS 密码访问、CSRF、禁止缓存、手机无横向溢出和无脚本错误。X 现有私密事件保持不变，按新测试 PID 重绑游戏与音频；远端播放器音轨检测到峰值 0.087。待测卡避免把已结束对局当作当前测试。以上为本地发布前包验收，未上传 Steam，敌人仍为 19 个默认 Regular Bot。

候选 04 的首局 `1789925760979` 实际走出 011 悬渊，随后在 027 碧海闯关记录刷新前快照 #2（预警剩余 62.57 秒）及刷新后快照 #3（进入坍缩阶段，剩余 44.52 秒），两次均有真实 M 地图截图。正式第 9、73 次请求 / 71 次接纳、输入释放成功；后续水中转移仍失败。第二局 `1789925932064` 正式第 19、24 发耗弹，证明反击发生但未证明战斗强度达标。阶段前后查看已得到首组实机证据，仍需跨多轮验证。

第三局 `1789925972280` 正式第 2、1 击杀、9 发耗弹、251 次请求 / 229 次接纳，输入释放成功；P1/P2 前后及 P3 预警的 M 地图检查有实机证据。长时间受阻于摆锤房，最终第三阶段每秒 6 点坍缩伤害耗尽生命。该成绩不能算第一，也不能证明胜率提升。

第四局 `1789926377629` 第 12、1 击杀、6 发耗弹，原验收程序单次读回未确认释放，批次因此停止。随后磁盘状态为手动、epoch 999、输入 0，但保留原失败记录，不追认通过。验收程序现最多等待 3 秒，每 50 ms 读取经过原身份和新鲜度校验的观测，仅接受本次手动 epoch 的零输入确认；超时仍失败。辅助程序散列也纳入版本身份。当前房间安全且存在安全出口时，新增规则排除进入正在坍缩房间的可选转移。类型检查与 173 项测试通过；新批次前两场（`1789926643431`、`1789926673828`）正式第 20、第 16，均获得新的释放确认。

## 历史迭代 · 2026-09-21

该轮本地候选为 `20260920-05`，尚未达成连续两次第一名。正式记录 `hybrid-smoke-1789920022258` 为第 3 名、1 击杀、8 发耗弹、283 次云端请求 / 251 次接纳、手动释放成功；随后一局第 19 名，再一局第 3 名（0 击杀）。失败和中断全部保留，不跳过失败凑连续胜场。

本轮修复了受伤后反复治疗、满弹匣 7 发霰弹枪反复换弹、目标短时间抖动、路径来回移动却无净进展、摆锤房路径拐角，以及目的房间已知风险排序。没有修改敌人数量、难度、伤害、生命值或结算。每个候选单独保存真实游戏画质配置：720p、60 帧上限，正常视距，减少阴影/反射开销；原 Steam 设置不受影响。测试期间不并行进行大型原生编译。

`scripts/validate-hybrid-series.cjs` 顺序运行完整比赛，达到同版本连续两次正式第一时停止；中断或未成功释放输入则停止批次。`scripts/hybrid-streak.cjs` 从全部按时间排列的结果重新计算连胜，版本身份包括原生包、运行时策略、测试程序和画质配置的散列。原始官方结果、云端参与和输入释放均须成立。

Studio 类型检查与完整测试 **155/155** 通过；私密控制台 Python 访问保护测试 **4/4** 通过。候选 05 的 Prepare → Cook → Build-snapshot → Stage 与 137 文件校验通过。官网密码控制台已通过真实 HTTPS 访问保护、手机布局和浏览器无脚本错误验收；X 私密输出已核对到当前测试游戏与虚拟头像，并加入每次测试进程变化后的画面/音频重新绑定。详见 [控制台说明](../console/README.md)。这些工程通过项不替代夺冠和长时间稳定性验收。

以下为早期轮次的历史记录；Steam 新版安装验收与连续两次第一仍是未完成项目。

## 结论

新架构已完成实现、编译和两局候选包实机测试：持续移动、跨房间、开火耗弹、结算及手动释放已得到证据。**两局均第 16 名，尚未达到超过现有 Bot 的目标。** 云端 Jev 在第二局真正参与战术决策，但没有击杀或开火，仍需优化补给、威胁发现和受攻击时的应对。

这两局是打包后游戏的发布前检查，使用候选版本 `20260920-02`；**不是 Steam 新版本安装验收**。Steam 安装仍为 BuildID `25420188`，新候选包尚未上传或切换任何 Steam 分支。没有用编辑器画面替代游戏实机验证。

## 已完成的工程检查

* Studio 类型检查通过，最新完整测试套件 145/145 通过；包括战术进展和有界租约恢复回归。
* Studio 前端及主进程构建通过。
* ETC Shipping、配套原生模块、冻结副本 Shipping 编译通过。
* ETC 发布工具与后端检查 100/100 通过。
* 候选包 Cook / Stage 成功，137 个运行文件、包内 AppID 和配置校验通过。
* 两个完整上传包分别校验 7 个和 130 个文件；未上传。
* 原生自动化测试入口尝试后没有返回有效测试报告，**不能计为通过**。仍需修复测试发现/执行环境并补验。

## 实机结果

| 项目 | 本地策略 + 共用执行器 | 云端 Jev + 共用执行器 |
| --- | --- | --- |
| 原始记录目录后缀 | `hybrid-smoke-1789915354075` | `hybrid-smoke-1789915590474` |
| 有效游戏位置采样 | 62 | 101 |
| 不同位置（0.1 cm 四舍五入） | 60 | 88 |
| 同房间采样轨迹长度 | 127.62 m | 223.1 m |
| 房间槽位 | 13 → 14 | 18 → 38 → 40 |
| 观测至控制发送 P95 | 50 ms | 49 ms |
| 游戏确认耗弹 | 5 发 | 0 发 |
| 击杀 | 0 | 0 |
| 官方结算 | 第 16 名 | 第 16 名 |
| 手动释放 | 通过 | 通过 |
| 云端请求 / 接纳回答 | 未调用 | 24 / 23 |
| 云端回答中位数 / P95 | 不适用 | 392 / 1167 ms |

轨迹长度仅累计同一房间内相邻采样的平面距离，排除跨房间传送；不是精确里程计。两局地图种子与遭遇不同，不能据此得出云端优于本地策略。第二局回答选择 17 次扫描探索、6 次转移；另有 1 次请求在约 1.2 秒超时，本地控制继续执行。原始记录保存在本机被 Git 忽略的 `test-results` 下，不含密钥。

早期检查中未获取前台焦点、加载阶段测试程序未正确处理重授权、旧候选进程已退出等测试准备问题已独立记录，未混入上述有效两局。底层租约和前台校验没有放宽。

## 尚未完成

2026-09-21 玩法上下文迭代：整理当前 001–028 房间的中英规则摘要，来源审计保存在本机；Jev 只获知当前和已到访类型。新增真实 M 地图快照、倒计时检查、跨房路线与私密控制台显示，详见[玩法知识](ROOM_KNOWLEDGE.md)。166 项 TypeScript 测试与 4 项控制台访问保护测试通过。

候选 `20260921-02` 的 `hybrid-smoke-1789923875621` 实际打开 M（3 个采样确认地图可见），取得 42 房快照，走过画廊与大电视；14 次云端调用携带对应房间规则，12 次战术回答被接受，近期成功响应约 397–512 ms。过门短暂停顿后，旧“传送中”反馈遮蔽了后续租约过期原因，验收程序保守停止；没有正式结算，不能计为胜局。已修正原生重复释放时的停止原因更新，并以新候选继续验证。250 ms 输入租约与人工接管限制保持不变。

用户追加的当前验收目标：本地同一候选版本连续两局正式结算第一名。当前未达成；下列改进继续按此标准迭代。

后续诊断中，`hybrid-smoke-1789916337173` 的 53 次有效云端回答全部为扫描，3 分钟超时；已加入探索进展、房间停留时间、近期掉血及 12 秒无新发现的扫描限制。`hybrid-smoke-1789916611837` 出现跨房间后停滞，截图及源码定位到 Room027 的直接移动排除了玩家控制器；该局同样未完成。

候选 `20260920-03` 已修复上述房间接入，并让 010/011/024/027 的逐帧移动遵守玩家自动控制租约。Shipping 编译、Cook、Stage 与 137 文件校验通过。正式长测中另发现租约中断，已加入仅针对同身份、同对局、持续前台且新鲜观测的有限恢复；所有中断都保留记录，不计胜利。后台编辑器曾占用约 78% GPU，最小化其窗口后降至约 4.5%，编辑器进程和开发状态保留。仍须以完整比赛结果验证改善。

1. 新构建从 Steam 安装、库内启动及云端 Jev 验收。
2. 原生自动化测试有效报告；40 条基础路线和 20 场完整比赛。
3. 改善开局补给、持续威胁发现和机关通行稳定性；已有正式击杀与冠军，但仍有多场开局死亡，不能假定已具备稳定高水平战斗能力。
4. 同种子、同装备、同信息条件的分档对战与 19 个 Pro 混战基准。达到统计门槛前不宣称超越 Bot。
5. 两小时完整直播及之后的 8/24/72 小时连续测试。

已有 Steam default/candidate 与线上服务器保持原版本。新包显示版本为 `Playtest 2026.09.20.02`，UE 联机校验也包含此字段；未经配套服务端验收不能直接替换现有线上版本。后续先走独立测试分支验证离线自动游玩。
