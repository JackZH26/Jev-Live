# 原生 Pro 托管玩家：实现与 Steam 验证

2026-09-24。原生 Pro 全权托管已实现：游戏接管提交 `5c7bd455`，战斗修正 `b9800a09`，水上乐园控制交还修正 `f4427b32`。当前实际运行 Steam 候选 `20260924-04` / BuildID `25508864`，Studio 续局修正 `8403f2d` / 策略 `271402348893`。最新 5 局完成，共 1 次玩家击杀、场均伤害 23.8、最好第 41 名，0 胜。完整接管和直播链路已通，争冠目标仍未达到；各版本独立统计，旧版第 6 名包含长时间停滞，不能作为提升证据。

## 结论

复用游戏完整的 Pro BOT AI 来操控直播角色，保留玩家控制器、身份、背包、HUD 和结算。JEV 负责开局、直播、连续对局和监控，战斗时只续期控制授权，不再逐条下发战术指令。必须使用含新接口的 Steam 安装包；旧包明确报不支持，不静默回退。

## 改动前核实的边界

以下路径相对于 ETC 工程 `Plugins/GameFeatures/EtcCore/Source/EtcCoreRuntime/`，描述当前查阅的源码；运行效果必须另以安装后的 Steam 包验收。

| 位置 | 已有行为与缺口 |
| --- | --- |
| `Public/AI/EtcBotTypes.h` | 难度枚举最高档为 Pro，高于 Expert。 |
| `Private/Development/EtcJevBridge.cpp`，`Executor` | 为玩家挂载 BOT Brain 与 Pro Aim，但通过 `SetExternalObjective` 执行外部目标。 |
| `Private/AI/EtcBotBrainComponent.cpp`，`Reevaluate` | 外部控制立即转入 `ReevaluateExternal`；完整自主决策要求 `AEtcBotController`。 |
| 同文件，`TickComponent` | 玩家控制器没有外部控制时直接退出，关闭外部控制不能自动变成完整 BOT。 |
| 同文件，`UpdateSurvival` / `UpdateRecovery` / `UpdateAutomaticTraversal` / `BeginStallWalkOff` | 外部模式跳过自动求生、恢复及部分攀爬脱困；这些行为目前不能靠 Pro Aim 恢复。 |
| 同文件，`RefreshPerception` | 原生视觉与受击感知依赖 BOT 控制器；包含无人机存活/活动状态、装备弹药不足时规避大型无人机等判断。 |
| `Private/Development/EtcJevController.h` | 已有保留玩家控制器的路径跟随适配，可扩展复用。 |

这解释了两种控制方式的功能差异，不能据此认定所有实测死亡都由同一个原因造成。

## 已实现的改动

1. **共用同一套原生大脑。** 将难度、所属房间、感知、阵营和移动控制等 BOT 控制器依赖收敛为共享适配接口。普通 BOT 保持原行为；托管玩家使用 Pro 配置，进入同一战术、补给、恢复、撤离和机关路径逻辑。不能只删类型判断，否则感知与路径依赖仍为空。
2. **保留玩家身份与镜头。** 保留 PlayerController、PlayerState、背包、技能系统、HUD 和结算归属；避免简单更换为 AIController 后破坏身份或相机。原生战斗和玩家镜头只能有一个控制来源，沿用平滑视角控制，验证转向不会妨碍瞄准和脱险。
3. **明确控制权。** 区分手动、外部目标、原生 Pro 三种模式。原生 Pro 接管后，Studio 停止发送 engage/cover/loot 等目标，只维持有时限的控制授权。手动接管、失焦、授权过期、换角色或离开对局时，游戏端释放路径、射击、开镜及移动输入。接口仍限定离线 BOT 对局。
4. **新增能力协商。** 桥接 v3 增加向后兼容字段 `capabilities.nativePro`，实际执行器回传 `control`、`tier`、`brainMode`、`nativeDecisions` 和目标。只有游戏确认 `native-pro` / `Pro` / `running` 才显示已接管；未启动或超时则交还手动。旧版 Steam 包明确显示“不支持原生 Pro”，不能静默回退后仍标为 Pro 全托管。
5. **复用正式开局与续局。** Studio 通过正式 BOT MATCH 按钮进入，开局选房仍走正常选择流程。失败后点击正式返回大厅；确认进入大厅后随机等待 5–8 秒，再匹配。加载时间单独统计，失焦暂停倒计时。
6. **完成 Steam 交付后验收。** 游戏端编译、测试和打包后，更新 Playtest 并从 Steam 安装；核对 BuildID 和运行进程。开发构建或编辑器结果不能代替用户要求的 Steam 实测，也不能把尚未安装的源码当作已上线能力。

已通过：Studio 类型检查与 303 项测试、游戏 21 项原生回归测试（包含控制权和租约释放）、407 项后端/交付工具测试，以及 Shipping 构建和 139 个交付文件校验。它们证明接口与打包回归通过，不代表真实战斗表现。

## 测试与判定

- 先验证控制权和身份：中途手动接管、失焦、会话断开、穿门、死亡、回大厅均无残留输入，伤害/击杀/名次归属于直播角色。
- 验证原生能力实际执行：低血掩体恢复、换弹换枪、宝箱补给、机械威胁、黄房撤离、攀爬与机关脱困；日志必须记录真实 Brain 模式及结果，不能仅记录客户端期望值。
- 完成至少 5 局真实 BOT 冒烟及连续续局，再对比 20 局；最终单独进行 100 局评估。统一敌人数量/难度和游戏规则，保留所有失败与异常。报告胜率、前十率、名次中位数、玩家击杀/伤害、无人机击杀和卡住时间；不混淆机械击杀与玩家击杀。
- Pro 是游戏最高难度档，接入完整能力不等于保证每局第一；先测原生基线，再只针对复现的问题优化。

## 已完成的 r3 对照样本

策略 `e0a63a278021`，提交 `f24a965`，Steam BuildID 25499909；5 局完整测试，无中断，0 胜。

| 局数 | 名次 | 玩家击杀 | 玩家伤害 | 存活秒数 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 47 | 0 | 38 | 20.059 |
| 2 | 49 | 0 | 0 | 18.498 |
| 3 | 50 | 0 | 12 | 8.258 |
| 4 | 43 | 1 | 103 | 38.062 |
| 5 | 43 | 0 | 33 | 46.303 |

原始记录：`test-results/steam-evaluation/pro-r3-1790249070403/summary.json` 与同目录逐帧记录（本机保留，不进入 Git）。名次中位数 47，平均伤害 37.2。该小样本不证明稳定提升，也未达到第一名目标。

## 原生接管第一轮：Steam 25504980

Steam 实际下载 139 个文件全部 SHA-256 匹配；游戏进程 33488 的父进程为 Steam。5 局无失焦或遥测中断，每局均有实际 `native-pro / Pro` 状态和原生决策计数；结束后手动释放成功，残留输入为 0。

| 局数 | 名次 | 玩家击杀 | 玩家伤害 | 存活秒数 | 原生决策数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 41 | 0 | 0 | 53.614 | 182 |
| 2 | 41 | 0 | 0 | 54.797 | 200 |
| 3 | 49 | 0 | 0 | 14.497 | 52 |
| 4 | 48 | 0 | 0 | 22.999 | 83 |
| 5 | 34 | 0 | 0 | 82.983 | 277 |

原始记录：`test-results/steam-evaluation/native-pro-r1-1790252910328/`。名次中位数 41，但零伤害说明不能判为战斗能力提升。观察到原生开箱、换枪、跨房、恢复和无人机交战状态。此处的无人机交战状态不等于击杀。

逐帧弹药观测显示五局分别射击 0、1、0、0、0 次；不能把零玩家伤害表述为完全未射击。后续采集器直接保存每局射击数，并新增独立统计脚本，保留未知字段和所有失败局。

四次实际大厅重新授权等待为 7.957、5.883、7.407、5.985 秒；到加载阶段还包括游戏自身 2–4 秒开局过渡，分开统计。

排查发现 `bEarlyBotSkirmish` 原先只判断对手是否 BOT。托管玩家面对 BOT 因而套用了开局搜刮优先和 0.45/3.5 秒的短交火窗口，但 BOT 面对玩家没有该限制。修正提交 `b9800a09` 要求双方身份均为 BOT，保持普通 NPC 互战节奏。新增身份组合回归通过。第一次回归有一项原有随机瞄准测试失败；无代码改动再次执行后 21 项全部通过，两份报告均保留，未把首次失败删除。

## 修正后的 Steam 冒烟：25507242

实际安装 139 个文件与冻结的发布产物匹配。运行进程 43168 来自 `D:\Steam\steamapps\common\Enter the Cube Playtest`，父进程为 Steam，游戏大厅显示 `Playtest 2026.09.24.03`。正式 default 分支仍为 `25499909`；本次只更新 candidate，联网服务器仍是网络版本 `20260924-01`，不能将离线 BOT 结果当作正式多人联网验收。

| 局数 | 名次 | 玩家击杀 | 玩家伤害 | 射击数 | 存活秒数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 48 | 0 | 24 | 7 | 19.403 |
| 2 | 43 | 2 | 209 | 10 | 51.743 |
| 3 | 48 | 0 | 36 | 3 | 10.338 |
| 4 | 32 | 0 | 0 | 9 | 95.510 |
| 5 | 47 | 0 | 40 | 2 | 27.210 |

原始记录：`test-results/steam-evaluation/native-pro-r2-1790259610690/`。5 局无中断、结束后释放输入成功；实际原生 Pro 状态覆盖约 95.3% 的比赛观测，其余主要是穿门等正常释放阶段。四次大厅重新授权等待为 5.775、6.974、7.079、6.426 秒，加载单独计时。名次中位数 47、总射击数 31；输出恢复不等于名次稳定提升，地图和遭遇并非配对对照。

X 与 YouTube 的新私密事件已分别验证播放器连续推进约 8 秒、视频帧增长和音频解码增长，OBS 画面只保留游戏与游戏声。证据在 `test-results/platform-playback-native-pro-r2.json`。另完成约 120 秒三路同时推流：X / YouTube 私密直播加 Twitch 带宽模式，6 次采样均无推流丢帧、拥塞或重连，各路均有真实游戏声。Twitch 测试随后停止、测试密钥已清除；回执在 `test-results/three-output-native-pro-r2.json` 与 `three-output-layouts-native-pro-r2.json`。不声称已完成 Twitch 观众播放或三路同时公开直播验收。

交付回执位于 ETC 的 `Saved/Playtest/Releases/20260924-03/`。网络上传遇到整包传输不完整后，使用 Steam 官方增量合并流程，以已验证的 default 为基准；最终 139 个文件的路径、大小、Steam SHA-1 与本地冻结 SHA-256 全部匹配后才发布 candidate。不能把复用的主 depot 清单写成一次新的整包上传成功。

## 20 局扩展测试与续局修正

同一 Steam `25507242` 与 Studio `fdc5da051f61`，扩展测试累计完成 20 局有效对局：0 胜、0 次前十、11 次玩家击杀、平均玩家伤害 57.8、名次中位数 46.5、最好第 35 名、平均存活 34.552 秒。实际射击 137 次，原生 Pro 状态覆盖约 97.65%。19 次重新授权等待在 5.338–7.964 秒内。

它不是一次无中断的 20 局运行：首批 9 局后，新增 Twitch OBS 实例抢占 Windows 焦点，另有一局第 40 名被记为中断，Studio 随后释放输入并停止自动控制。恢复焦点后单独完成剩余 11 局。两个原始报告和中断局全部保留，不改写首批的 `complete:false`。汇总脚本拒绝跨版本、跨策略和重复对局合并。

- 首批：`test-results/steam-evaluation/native-pro-r2-long-1790259974196/`
- 恢复后：`test-results/steam-evaluation/native-pro-r2-resume-1790260590838/`
- 扩展汇总：`test-results/native-pro-r2-extended-aggregate.json`
- 包含冒烟的 25 局汇总：`test-results/native-pro-r2-25-aggregate.json`，共 13 次击杀、场均伤害 58.6、最好第 32 名，仍无胜局。

实测确认了原生换弹及治疗：恢复后第 3 局，手枪弹匣从 0 补至 12；纱布数量从 1 减至 0，生命随后从 20.71 逐步恢复到 34.71。该局仍然失败；治疗执行成功不等于生存策略合格。

同一局也发现一个续局边界：淘汰后残留纱布治疗令原始生命值变为 2，游戏桥接短暂从 `dead` 切回 `playing` 并将 `result` 清空。若首次返回按钮尚未出现，旧 Studio 会停止重试。修正 `8403f2d` 在已确认结算、仍等待大厅时继续重试真实识别到的返回按钮；保留身份、时限、前台和按钮新鲜度校验，期间不恢复战斗输入。该复现用例先失败、修正后通过；完整 304 项测试与类型检查通过。

桌面包 `release/native-pro-20260924-r2-lobby/win-unpacked` 已部署到 `release/current/win-unpacked`，策略标识 `271402348893`；Steam 游戏仍为 `25507242`。该改动只修正续局，不把旧策略的统计混入新的验证批次。100 局最终评估未执行，当前结果不支持宣称已具备稳定争冠能力。

## 新桌面五局验证与水上乐园停滞

策略 `271402348893` 的五局均完成，无失焦中断，结束后输入释放成功：名次 47、46、41、45、6；总计 2 次玩家击杀，场均伤害 97.6，0 胜。四次大厅等待实测 7.849、7.399、7.829、8.066 秒（采样允许 250 毫秒误差），加载另计。原始记录为 `test-results/steam-evaluation/native-pro-r2-lobby-1790261356129/`。

第 6 名不能作为战术提升证据：该局在 Room027 水上乐园有连续 548.546 秒的原地战斗状态，目标在约 6–7 米外，原生决策仍增长，射击数保持 1，最终房间坍塌时淘汰。完整结算仍计入统计；逐帧证据另存 `test-results/waterpark-native-stall.json`。

源码发现水上乐园路径到达最终平台后仍返回 `bOverrideGoal=true`，通用大脑因此持续判为机关避险，压制瞄准和开火。新增真实路径提供器回归测试，在旧实现上出现三次“到达后未释放战斗抑制”的断言失败。修正方向是到达后归还战术控制，新的跨平台路径仍保持避险；候选发布与实机复测结果另行记录，不能以单元测试替代 Steam 验收。

## 水上乐园修正交付与 Steam 25508864 实测

提交 `f4427b32` 在最终平台到达后释放路线目标覆盖与直接移动，使正常战术重新管理瞄准和射击；切换到新的跨平台目标时仍启用避险。真实路径提供器测试旧代码失败三次断言，修复后 6 项原生聚焦回归通过；后端及交付工具 421 项、上传工具 6 个场景通过。没有改变对手数量、难度、生命或武器伤害。

Shipping 发布 `20260924-04` 已上传 candidate，Steam BuildID `25508864`。本次运行 depot 使用完整 Standard ZIP，790 个 HTTP 请求全部成功；主 depot 的 7 个文件逐一校验一致后复用上一版完整 Standard 清单。提交前校验全部 139 个服务端文件。Steam 客户端下载安装后 139 个 SHA-256 全部匹配，再通过 `steam://run/5272970` 启动实际安装目录的游戏；进程 43740，父进程 Steam 48972。实际大厅版本 `2026.09.24.04`，英语及法语更新说明、返回大厅均验证，并恢复英语。default 仍为 `25499909`，正式联网服务器未更新；本次验收仅限离线 BOT。

| 局数 | 名次 | 玩家击杀 | 玩家伤害 | 射击数 | 存活秒数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 48 | 0 | 23 | 3 | 16.875 |
| 2 | 41 | 1 | 60 | 9 | 52.299 |
| 3 | 47 | 0 | 0 | 0 | 20.660 |
| 4 | 50 | 0 | 12 | 3 | 13.937 |
| 5 | 50 | 0 | 24 | 2 | 7.084 |

原始批次 `test-results/steam-evaluation/native-pro-r3-1790264198241/`，统计 `test-results/native-pro-r3-aggregate.json`。5 局完整、0 中断、结束释放输入成功；原生 Pro 状态覆盖约 99.41%，名次中位数 48，平均存活 22.171 秒。四次大厅等待 7.398、7.519、6.321、5.340 秒，加载另计。测试后恢复自动连续对局。

本轮实际遇到房型 025、002、020、009、008，**未遇到水上乐园 027**。因此这里只能确认修正版 Steam 包及常规对局运行，不能声称旧停滞场景已在 Steam 重现并解除。原生回归证明路线交还的代码行为，不替代该场景实机验收。5 局没有胜利，战斗性能不通过目标验收；100 局最终评估仍未执行。

当前 X 与 YouTube 继续使用原私密事件，已复查各自播放器推进约 8 秒，真实游戏画面、游戏声与隐藏主播。平台回执 `test-results/platform-playback-native-pro-r3.json`；OBS 回执 `test-results/pure-gameplay-outputs.json`。Twitch 仍仅有此前 120 秒三路同时带宽测试，不是公开直播或观众端播放验收。

后续优先核查受击后的感知与搜刮切换。本轮有多次掉血仍处于 Loot 的记录；这不直接证明伤害来自屏幕中的某个敌人。源码中 PlayerController 配置了 Damage sense，但 UE 的伤害事件分发还要求 `IAIPerceptionListenerInterface`，当前玩家控制器没有该接口。这是需要独立复现的适配缺口，尚未作为本版已修复项目；还需区分武器伤害是否上报、遮挡和房间机关伤害，不能把全部死亡归结为同一原因。
