# Steam Playtest 2026.09.21.05 发布验收

[English](STEAM_PLAYTEST_20260921.en.md) · 作者：[JackZ / X](https://x.com/jackzhj)，欢迎关注交流。

2026-09-21，Enter the Cube Playtest **5272970** 的 **default、candidate** 均已更新为 Steam **BuildID 25425783**。大厅版本为 **Playtest 2026.09.21.05**，Windows Shipping 客户端、Linux 专服和后端的网络版本统一为 `etc-playtest-20260921-05`。主游戏 AppID 5030940 未发布。

本次发布当前游戏原生执行器与地图观察接口。Jev 的房间知识、倒计时规划和守备策略仍由 Jev Live 提供；Steam 包更新不等于自动开启 Jev 服务。

## 验收结果

- 两个完整 depot 的官方清单与不可变发布包逐项匹配：137 个文件，路径、大小、Steam SHA-1、本地 SHA-256 均通过。
- Steam 客户端从旧 BuildID 25420188 实际升级，下载 84,240,160 字节；安装后的全部 137 个文件再次通过校验。
- 从 Steam 启动安装目录中的游戏，确认父进程为 Steam、大厅版本正确、真实 Steam 登录及原有 EA 资格可读取。
- BOT 对局加载、移动、射击（弹匣 12→11）、M 地图开关、ESC 菜单及返回大厅通过；F2 未打开调试入口。
- 后端与发布工具 100 项、Studio 177 项、上传传输 6 项测试通过；类型检查通过。匹配专服启动成功并持续发送新版本心跳。

## 发布范围与限制

本次完成版本更新。Valve 的生成版本审核仍在队列中，Steam Playtest 仍为 **不可玩 / 报名隐藏**；开发者账号的成功启动不代表普通参与者已经可以进入。没有扩大访问范围、重置参与者或更改邀请配额。

专服保持原有维护状态（drain）。客户端联机入口正确显示“Servers are preparing”，未进行三名真人的完整联机验收。发布前同一原生包完成一次本地 Jev 完整对局，正式第 13 名、输入释放确认通过；不能计作 Steam 安装版完整对局或连续两次第一。连胜目标仍未达成。

旧 Steam BuildID **25420188** 与对应专服包保留用于回退。第一次内容 depot 服务端处理失败，重新上传后成功；最终只提交两份校验通过的完整清单。没有发布中间或不完整版本。

安装、启动、分支及专服回执保存在本机 `C:/LYRAETC/Saved/Playtest/Releases/20260921-05/`。含邀请码的验收截图只留本地；账号、密钥、会话凭据和原始私密截图不提交 Git。
