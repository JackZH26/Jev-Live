# 战术与镜头实机验收交接

[English](COMBAT_ACCEPTANCE_HANDOFF.en.md) · [实现说明](COMBAT_STRATEGY.md)

2026-09-21 更新：当前策略为 `7db03bed26ce`，实现提交 `0c80ed6`，连续演示驱动为 `7e22018`。最新桌面包在 `release/combat-20260921-r5/win-unpacked`，已验证实际启动、依赖和包内 7 个策略模块。配套本地游戏候选为 `20260921-10`，EXE SHA-256：`8d284b8ffe40448ae7139df80eb6a558e555c195f48b9c919d24ff6c140d2fe3`；Shipping、Cook、pak 配置与 137 项文件校验通过。Steam 安装未替换。

新版本 214 项桌面测试和类型检查通过；实际 C++ 跟踪算法对 12°/s 移动目标在 30/60/120 Hz 下的稳定误差为 0.25/0.15/0.10°，保留原转速与加速度约束。新增实际持枪距离、近身自动枪/远处狙击、已装弹备用枪、火箭安全距离、切枪释放旧 ADS 和掩体还击窗口。

候选 10 的完整单局结果分别是第 11 名/0 击杀/0 伤害与第 10 名/0 击杀/12 伤害，两次均确认人工输入释放。第一局暴露冰地路线长期停滞；第二局有 24 次射击。连续演示首局为第 14 名/0 击杀/73 伤害；后续尚未结算的击杀不作为最终成绩。没有通过夺冠或连胜验收。

原私密 X 场次已经结束，新的场次明确选择 Private 后启动，实际播放端已确认新标题、持续视频与递增音频解码量。私密链接和详细回执仅存本机。运行中的旧桌面窗口仅负责 OBS，新测试驱动负责游戏 AI。演示最多 30 分钟、1200 次 JEV 请求或 20 局；失焦立即释放输入，采集守卫隐藏过期画面，监控随后停止推流。连续演示不能替代逐局释放确认的两连胜验收。语音仍存在延迟，未通过真人主持验收。

下文是候选 07/08 的历史交接。继续工作应以候选 10 和上述新策略为起点，优先修复路线与补给阶段停滞，再做完整同版本成绩验证。

## 已交付

- JEV 战术约束、Bot 原生执行和连续镜头控制；包括正常选房、补弹、恢复、较弱主武器替换、接近掩体蹲走与 ADS。
- 212 项桌面测试通过；实际 C++ 镜头算法通过 30/60/120 Hz、速度与加速度、反向、角度跨界、卡帧、手动释放和降低速度上限的检查。
- 原生候选 `20260921-08`：Shipping 编译、资源处理、包内 Steam 配置和 137 项文件校验完成。游戏 EXE SHA-256：`ae7dce6349cc64d8b13f25b2c747c879d374737e8a148aec041dacf1e79beffc`。
- 桌面 AI 指纹 `f4889da1fafd`，基于 `395b1b4` 的独立干净依赖构建；已核对包内 7 个策略模块、依赖和实际窗口启动。早期使用依赖目录链接的两个包缺依赖，不能交付。可用包目录为 `release/combat-20260921-r3/win-unpacked`，后续采集修复包以最新构建回执为准。

## 实测结果

| 候选 | 结果 | 证据与限制 |
|---|---|---|
| 20260921-07 | 第 16 名，0 击杀，伤害 9 | 完整正式结算；Jev 27 请求、23 返回；结束后手动释放确认。已观察到选房、蹲走、ADS；643 对连续镜头采样没有超出速度上限加采样容差的异常跳转 |
| 20260921-08 第一次 | 中断 | 进入比赛前失焦，0 次 Jev 请求，不计成绩；已释放输入 |
| 20260921-08 第二次 | 中断 | 开局加载时失焦，0 次 Jev 请求，不计成绩；已释放输入 |

07 暴露的远处掩体与丢失视野问题已经进入 08：拒绝不合适的远处掩体建议、短时间保持同一掩体目标、撤退时回看实际见过的敌人方向。08 尚无完整对局，不能声称更高胜率、连胜或更多击杀。

## 采集问题与当前状态

游戏退出后，临时演示脚本曾把 OBS 匹配优先级设为 0，导致同类窗口被捕获。已停止推流、隐藏游戏图层并静音游戏音频。临时脚本改为按可执行文件匹配（2），增加新鲜游戏观测和前台校验；正式软件重连现有 OBS 输入时也重新设为 2。没有发送崩溃报告，没有改 X 事件的私密属性。

OBS 的分类依据见 [官方窗口匹配实现](https://github.com/obsproject/obs-studio/blob/master/libobs/util/windows/window-helpers.c)：按窗口类匹配允许其他进程，按可执行文件匹配拒绝不同程序。单靠同类窗口不能证明画面来自目标游戏。

## 下一轮

需要游戏连续保持前台约 5–10 分钟。依照采集技能，连续采集失败后先交接，不能无休止重新抢焦点。

1. 在前台条件稳定后，以冻结候选 08 和相同桌面 AI 指纹重新开始；先记录实际 PID、EXE 散列、前台与 OBS 目标。
2. 先确认短探针确实是游戏画面，再接回原来的私密 X 事件。失焦或观测陈旧应隐藏游戏采集，不计入自动对局。
3. 分别验证安全补弹、恢复读条及打断、满背包换枪、受击找掩体后还击。没有遇到狙击枪或攀爬场景，不能宣称这两项已实测通过。手雷尚未进入新的战术接口。
4. 完成多局正式结算，分别记录名次、击杀、伤害和镜头轨迹；若继续连胜验收，必须同一原生、策略、图形与测试版本，且每局结束确认输入释放。
5. Steam 线上版本仍未替换。候选包验证通过后，再按游戏发布流程处理更新。
